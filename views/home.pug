extends layouts/main
include mixins/videoblock

block content
    .videos 
        each item in videos 
            +videoBlock ({
                videoFile:item.fileUrl,
                id:item.id,
                title:item.title,
            })